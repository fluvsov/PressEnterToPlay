# PressEnterToPlay
A Python console game of chance. Press Enter to get a number from 0 to 9999. You win if the digits match completely (like 777 or 4444). Otherwise, you lose. The game runs in an infinite loop until you close the terminal.
